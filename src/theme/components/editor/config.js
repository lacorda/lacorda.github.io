export const MONACO_EDITOR_CONFIG = {
  defaultLanguage: 'javascript',
  defaultValue: '',
  options: {
    minimap: {
      autohide: true,
      enabled: false,
    },
    mouseWheelZoom: true,
    padding: {
      top: 80
    },
    smoothScrolling: true,
    // suggestFontSize: 30
  },
}
